import React from "react";
import { Toolbar, AppBar, Typography } from "@material-ui/core";

export const NavBar = () => {
  return (
    <div>
      <AppBar>
        <Toolbar>
          <Typography color="inherit">
            menu delle armi che guarda e pazzesco e presto o tardi scende lo
            giuro
          </Typography>
        </Toolbar>
      </AppBar>
    </div>
  );
};
